package com.timegun.mod.item; // Make sure this matches your actual package

import net.minecraft.world.item.Item;

public class TimeGunItem extends Item {
    public TimeGunItem(Properties properties) {
        super(properties);
    }
}